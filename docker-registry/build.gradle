import com.bmuschko.gradle.docker.tasks.image.*

task buildImage(type: DockerBuildImage) {
    dockerFile file('src/main/docker/Dockerfile')
    inputDir = project.projectDir
    tag = 'vg/docker-registry:latest'
}
